# Lessons

- When the user asks to implement an approved plan after a planning turn, wait for Default mode if required by the environment, then execute the implementation directly instead of producing another plan.
- Before switching away from a detached dirty worktree, save the current state in a named stash and confirm the target `origin/main` commit.
